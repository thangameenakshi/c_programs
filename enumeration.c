#include <stdio.h>

 enum bool{NO,YES};
int main()
{
    enum point{x=10,y};
    enum bool a;
    enum bool b;
    b=y;
    a=YES;
    printf("A: %d",a);
    printf("\n B: %d",b);
    return 0;
}
